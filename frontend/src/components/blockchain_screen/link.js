import { Box } from "@mui/material"
import InsertLinkIcon from "@mui/icons-material/InsertLink"

function Chain() {
  return (
    <Box sx={{ display: "flex", justifyContent: "center", my: 2 }}>
      <InsertLinkIcon sx={{ fontSize: 40, color: "#1a237e" }} />
    </Box>
  )
}

export default Chain

